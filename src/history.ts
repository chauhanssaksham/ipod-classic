import { createBrowserHistory } from 'history';
//This module helps in utilizing the javascript history API throughout the app, using react-router v5, 
//to navigate to pages programatically (using javascript instead of user-driven events)
//This is needed
export default createBrowserHistory();